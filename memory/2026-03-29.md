# 2026-03-29

## LagerPro MVP - Arbeid

### Hva ble gjort

**Backend:**
- `UpdateMottakLinjeGodkjenningHandler` — ny command for godkjenning av enkeltlinjer i mottak (PATCH `/mottak/{id}/linjer/{linjeId}/godkjenning`)
- `UpdateMottakLinjeRequest.cs` — ny request-contract
- `ReceiptsController.cs` — lagt til nytt endepunkt for linje-godkjenning
- `UpdateLeveringStatusHandler` — lagt til ekte lager-validert lagerplukk (kaster `InvalidOperationException` hvis artikkel mangler eller har for lite beholdning)
- `ShippingController.cs` — fang opp `InvalidOperationException` og returner 400 Bad Request
- `Microsoft.EntityFrameworkCore.Design` 8.0.x installert i LagerPro.Api

**Frontend:**
- `mottak/page.tsx` — lagt til per-linje Godkjenn/Avvis-knapper når mottakstatus er "Mottatt", oppdatert API-import
- `lib/api.ts` — lagt til `UpdateMottakLinje`-interface
- `artikler/page.tsx` — komplett rewrite: type-filter dropdown, type-badge chips, vis-inaktive checkbox, artikkel-telling, kategori-datalist, flere enheter (PAKKE, BOKS), pris-visning

**Bygget:**
- Backend: `dotnet build` ✅
- Frontend: `npm run build` ✅

**Commit:** `6c88786` — pushet til GitHub

### Kjent status
- Migrations: Finnes allerede i `src/LagerPro.Infrastructure/Migrations/` (InitialCreate fra 2026-03-27)
- EF Core Design package ble lagt til i dag (trengs for `dotnet ef` CLI)

### Gjenstår
- Mottak: Når mottak settes til "Godkjent" → auto-opprett lagerbeholdning for alle godkjente linjer (PT-1: sjekk om dette allerede skjer i UpdateMottakStatusHandler)
- Produksjon: Start-status bør validere at resepten er aktiv
- Resept-sida: kunne trengt bedre feilhåndtering
- Test-dekning
